/** Tailwind CSS v4 is configured in CSS (see app/globals.css), so this is the only build hook it needs. */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
